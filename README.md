# isaac

Isaac is a coding client and agent implementing the [Agent Client Protocol (ACL)](https://agentclientprotocol.com/).

To run the client with the agent: `uv run python -m isaac.client uv run isaac`
